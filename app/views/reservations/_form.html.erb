<%= form_for (:reservation)  do |f| %>

	<%= f.label :guests, "How many are you?" %> <%= f.number_field :guests %><br/>
	<%= f.label :check_in %> <%= f.date_field :check_in %><br/>	
	<%= f.label :check_out %> <%= f.date_field :check_out %><br/>
	<%= f.submit %>
 
<% end %>
